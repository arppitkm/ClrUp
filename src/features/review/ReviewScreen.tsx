import React from 'react';
import { PhasePlaceholder } from '../common/PhasePlaceholder';

export const ReviewScreen: React.FC = () => (
  <PhasePlaceholder title="Review" summary="Exactly what will be removed and how much space it frees. Nothing is deleted before this screen." phase="Phase 6" />
);
