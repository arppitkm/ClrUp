import React from 'react';
import { PhasePlaceholder } from '../common/PhasePlaceholder';

export const LargeVideosScreen: React.FC = () => (
  <PhasePlaceholder title="Large Videos" summary="Videos sorted largest first, with inline preview before you decide." phase="Phase 3" />
);
