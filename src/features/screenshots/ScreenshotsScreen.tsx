import React from 'react';
import { PhasePlaceholder } from '../common/PhasePlaceholder';

export const ScreenshotsScreen: React.FC = () => (
  <PhasePlaceholder title="Screenshots" summary="Every screenshot in one grid, with select-all by date section." phase="Phase 3" />
);
