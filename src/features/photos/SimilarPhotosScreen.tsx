import React from 'react';
import { PhasePlaceholder } from '../common/PhasePlaceholder';

export const SimilarPhotosScreen: React.FC = () => (
  <PhasePlaceholder title="Similar Photos" summary="Groups of near-identical shots, with the sharpest one marked to keep." phase="Phase 4" />
);
