import React from 'react';
import { PhasePlaceholder } from '../common/PhasePlaceholder';

export const DuplicateContactsScreen: React.FC = () => (
  <PhasePlaceholder title="Duplicate Contacts" summary="Contacts that look like duplicates, merged by default rather than deleted." phase="Phase 5" />
);
